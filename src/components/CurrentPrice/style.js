import { StyleSheet } from 'react-native'

const styles = StyleSheet.create({
    headerPrice: {
        width: '100%',
        height: "auto",
        alignItems: 'center',
        marginBottom: 20
    },
    currentPrice: {
        color: "#ffc600",
        fontSize: 32,
        fontWeight: 'bold',
        paddingTop: 20
    },
    textTitle: {
        color: "#ffffff",
        fontSize: 16,
        fontWeight: 'bold'
    }
})

export default styles;